#include<stdio.h>
#include<iostream>
using namespace std;

int main(void)
{
    int m, n, max, min;
    cin >> m >> n;
    int array[m][n];
    for (int i = 0; i < m; ++i){
        for (int j = 0; j < n; ++j){
            cin >> array[i][j];
        }
    }
    // check max
    
    return 0;
}
